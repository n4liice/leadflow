# ============================================
# LeadFlow Pro - Variáveis de Ambiente
# ============================================
# Copie este arquivo para .env e preencha os valores

# ============================================
# SUPABASE (obrigatório)
# ============================================
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sua-anon-key-aqui

# ============================================
# CLOUDFLARE WORKER PROXY (obrigatório em produção)
# ============================================
# URL do Worker que faz proxy para os webhooks do n8n
# Isso mantém as URLs do n8n seguras e não expostas no frontend
VITE_WEBHOOK_PROXY_URL=https://leadflow-webhook-proxy.seudominio.workers.dev

# ============================================
# IMPORTANTE - SEGURANÇA
# ============================================
# NÃO adicione URLs do n8n ou secrets aqui!
# Eles devem ficar APENAS no Cloudflare Worker
#
# O que NÃO deve estar aqui:
# - VITE_N8N_WEBHOOK_LAUNCH (mover para Worker)
# - VITE_N8N_WEBHOOK_PAUSE (mover para Worker)
# - VITE_N8N_WEBHOOK_RESUME (mover para Worker)
# - VITE_N8N_WEBHOOK_SECRET (mover para Worker)
